# Security Policy

## 公开仓库边界

- 不提交 `.env`、API Key、Token、密码、飞书 `open_id` 或企业真实业务数据。
- 不提交 SQLite 数据库、运行日志、聊天记录、审计记录和失败发件箱。
- `.env.example` 只保存变量名称和无效占位值。
- 演示数据库通过 `scripts/generate_demo_data.py` 在本地生成。

## 漏洞反馈

如发现安全问题，请不要在公开 Issue 中粘贴密钥、数据样本或可直接利用的细节。请通过 GitHub 账号主页提供的联系方式联系维护者。

## 生产使用提醒

本仓库为求职展示与技术原型。生产部署至少需要补充企业 SSO、服务端权限校验、数据库隔离、集中密钥管理、HTTPS、网络边界、日志脱敏、备份恢复和安全评估。

